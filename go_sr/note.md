- Generates exectuable that can be run on any system ( there is no need for a go dependency )
- every .go file start with the package declaration ( defining the package where it belogs to )
- supports utf-8
- variables and functions whose name starts with small letter is encapsulsated inside that module, means we cannot improt to an outside module
- we cannot use the valrus operator outside a function  


